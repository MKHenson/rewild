services:
  db:
    image: postgres:16
    environment:
      POSTGRES_DB: rewild
      POSTGRES_USER: rewild
      POSTGRES_PASSWORD: rewild
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
